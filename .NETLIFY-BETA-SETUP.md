# ⚠️ IMPORTANTE: CONFIGURAZIONE NETLIFY

## 🔴 AZIONE NECESSARIA PER ATTIVARE BETA MODE SU PRODUCTION

Il file `.env.local` NON viene caricato su GitHub (è in .gitignore per sicurezza).

Netlify deve leggere la variabile d'ambiente per attivare la beta mode.

---

## 📋 COSA FARE ORA:

### 1️⃣ Vai su Netlify Dashboard
```
https://app.netlify.com/sites/[TUO-SITO]/configuration/env
```

### 2️⃣ Aggiungi Environment Variable

**Key**: `NEXT_PUBLIC_FREE_BETA_MODE`  
**Value**: `true`  
**Scopes**: Production, Deploy Previews, Branch Deploys

### 3️⃣ Trigger Redeploy

Clicca su **"Trigger deploy"** → **"Deploy site"**

OPPURE

Fai un commit vuoto:
```powershell
git commit --allow-empty -m "trigger: redeploy for beta mode" ; git push origin main
```

---

## ✅ VERIFICA

Dopo il deploy, visita il sito e controlla:

- [x] Banner verde "Beta Gratuita Attiva" visibile in dashboard
- [x] `/pricing` mostra messaggio "Beta Gratuita Attiva" invece dei prezzi
- [x] `/redeem` mostra messaggio "Beta Gratuita Attiva" invece del form
- [x] Tutti gli utenti possono accedere a quiz con 20 domande/30 minuti
- [x] Link "Scopri i Piani" e "Attiva Codice" NON visibili

Se qualcosa non funziona, cancella cache Netlify e redeploy.

---

## 🔄 PER RIPRISTINO FUTURO

Quando vorrai tornare alla modalità commerciale:

1. Vai su Netlify Environment Variables
2. Cambia `NEXT_PUBLIC_FREE_BETA_MODE` da `true` a `false`
3. Oppure cancella completamente la variabile
4. Redeploy site

---

**Data**: 19 Febbraio 2026
